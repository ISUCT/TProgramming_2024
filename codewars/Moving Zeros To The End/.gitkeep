def move_zeros(lst):

    result = []
    zero_count = 0

    for num in lst:
        if num == 0:
            zero_count += 1
        else:
            result.append(num)

    result.extend([0] * zero_count)

    return result
